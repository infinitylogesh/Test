package prime

import com.workday.montague.parser.TokenMatcher

/**
  * Created by prime on 16/3/17.
  */

/*object LocationMatcher extends TokenMatcher[Int]{
  def apply(str: String) = {
    try {
        val
        Seq(Integer.parseInt(str)+1)
      else
        Seq(Integer.parseInt(str))
    } catch {
      case nfe: NumberFormatException => Nil
    }
  }
}*/
